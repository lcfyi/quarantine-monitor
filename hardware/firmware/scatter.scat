; This file tells the compiler's linker where in memory the program
; should be downloaded to. 
HPSSDRAM 0x00000000 0x40000000 ; A memory block name followed by a start address and size in hex
{
	APP_CODE +0				   ; code loaded at base + 0
	{
		* (+RO, +RW, +ZI)	   ; read only, read write and zero initialized
	}
	
	ARM_LIB_STACKHEAP +0 EMPTY 0x10000 ; A size for the stack and heap (64KB) at the top of memory
	{ }
}